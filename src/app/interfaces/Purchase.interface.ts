import { cars } from "./cars.interface copy"
import { user } from "./user.interface"

export interface Purchase { //Interfaz de marca
    id:number
    num_bastidor:cars
    username:user
    date:string
}